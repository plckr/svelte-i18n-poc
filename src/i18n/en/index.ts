import type { BaseTranslation } from '../i18n-types';

const en = {
	WELCOME: 'Welcome to {name:string}',
	VISIT_WEBSITE: 'Visit <>{url:name}<> to read the documentation'
} satisfies BaseTranslation;

export default en;
