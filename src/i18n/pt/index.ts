import type { BaseTranslation } from '../i18n-types';

const pt = {
	WELCOME: 'Bem-vindo a {name:string}',
	VISIT_WEBSITE: 'Visita <>{url:name}<> para ler a documentação'
} satisfies BaseTranslation;

export default pt;
